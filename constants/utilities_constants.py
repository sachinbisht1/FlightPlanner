"""All constants of utilities."""
from dotenv import load_dotenv
import os
load_dotenv()

REFRESH_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7 days
FOCAL_INFO = "focal_lenght"
IMAGE_HEIGHT = "image_height"
IMAGE_WIDTH = "image_width"
